const base = 5;
const heigth = 8;
const area = base * heigth;
const perimeter = base + base + heigth + heigth;

console.log('A área do retângulo é de ' + area + ' e o seu perímetro e de ' + perimeter + '.');